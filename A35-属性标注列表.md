# A35-属性标注列表

| **一级属性** | **二级属性** | **优先级** |
| --- | --- | --- |
| **上衣颜色** | `**unknow**`**,** `**black**`**,** `**white**`**,** `**gray**`**, red,** `**yellow**`**,** `**green**`**,** `**blue**`**,** `**purple**`**,** `**pink**`**,** `**orange**`**,** `**brown**` | **P0** |
| **下衣颜色** | `**unknow**`**,** `**black**`**,** `**white**`**,** `**gray**`**,** `**red**`**,** `**yellow**`**,** `**green**`**,** `**blue**`**,** `**purple**`**,** `**pink**`**,** `**orange**`**,** `**brown**` | **P0** |
| **手挎物体与否** | `**unknow**`**,** `**none**`**,** `**with**` | **P0** |
| **背部背物体与否** | `**unknow**`**,** `**none**`**,** `**with**` | **P0** |
| **上衣类型** | `**unknow**`**,** `**shirt**`**,** `**jacket**`**,** `**vest**`**,** `**hoodie**`**,** `**sweater**`**,** `**reflective_vest**`**,** `**camisole**`**,** `**t_shirt**`**,** `**dress**`**,** `**naked**` | **P1** |
| **下衣类型** | `**unknow**`**,** `**jeans**`**,** `**skirt**`**,** `**shorts**`**,** `**dress**`**,** `**pants**` | **P1** |
| **下身长裤/短裤** | `**unknow**`**,** `**long**`**,** `**short**` | **P1** |
| **手挎物体类型** | `**unknow**`**,** `**none**`**,** `**handbag**`**,** `**shoulder_bag**`**,** `**plastic_bag**`**,** `**paper_bag**`**,** `**shopping_bag**`**,** `**box**`**,** `**luggage**`**,** `**umbrella**`**,** `**folder**`**,** `**phone**`**,** `**other**` | **P1** |
| **手挎物体颜色** | `**unknow**`**,** `**black**`**,** `**white**`**,** `**gray**`**,** `**red**`**,** `**yellow**`**,** `**green**`**,** `**blue**`**,** `**purple**`**,** `**pink**`**,** `**orange**`**,** `**brown**` | **P1** |
| **背部背物体类型** | `**unknow**`**,** `**none**`**,** `**backpack**`**,** `**shoulder_bag**`**,** `**messenger_bag**`**,** `**other_bag**`**,** `**other**` | **P1** |
| **背部背物体颜色** | `**unknow**`**,** `**black**`**,** `**white**`**,** `**gray**`**,** `**red**`**,** `**yellow**`**,** `**green**`**,** `**blue**`**,** `**purple**`**,** `**pink**`**,** `**orange**`**,** `**brown**` | **P1** |
| **是否抱着东西** | `**unknow**`**,** `**none**`**,** `**with**` | **P1** |
| **帽子颜色** | `**unknow**`**,** `**black**`**,** `**white**`**,** `**gray**`**,** `**red**`**,** `**yellow**`**,** `**green**`**,** `**blue**`**,** `**purple**`**,** `**pink**`**,** `**orange**`**,** `**brown**` | **P2** |
| **戴眼镜与否** | `**none**`**,** `**with**` | **P2** |
| **蒙面与否** | `**none**`**,** `**with**` | **P2** |
| **上身长袖/短袖** | `**unknow**`**,** `**short_sleeve**`**,** `**long_sleeve**` | **P2** |
| **鞋子颜色** | `**unknow**`**,** `**black**`**,** `**white**`**,** `**gray**`**,** `**red**`**,** `**yellow**`**,** `**green**`**,** `**blue**`**,** `**purple**`**,** `**pink**`**,** `**orange**`**,** `**brown**` | **P2** |